capital: 100000
confidence_threshold: 0.58
mode: paper
nature:
  immune_z_threshold: 4.0
  pheromone_evaporation: 0.9
news_feeds:
- https://feeds.reuters.com/reuters/businessNews
- https://economictimes.indiatimes.com/markets/rssfeeds/1977021501.cms
- https://www.moneycontrol.com/rss/marketreports.xml
option:
  min_delta: 0.45
  risk_free_rate: 0.068
regimes:
  choppy:
    confidence_threshold: 0.55
    daily_max_loss_pct: 2.0
    min_delta: 0.45
    reward_risk_min: 1.5
    risk_per_trade_pct: 1.5
    theta_timeout_minutes: 60          # applies to the CHOPPY-regime debit-spread fallback (directional-style exit)
    credit_theta_timeout_minutes: 180  # iron condors: theta IS the edge — needs hours, not 60 min, to materialize
    credit_theta_loss_tolerance_pct: 15  # only timeout a condor if it's genuinely deteriorating (>=15% of max loss), not just flat
  high_volatility:
    confidence_threshold: 0.65
    daily_max_loss_pct: 4.0
    min_delta: 0.4
    reward_risk_min: 1.8
    risk_per_trade_pct: 2.0
    theta_timeout_minutes: 90
  trending:
    confidence_threshold: 0.58
    daily_max_loss_pct: 3.0
    min_delta: 0.4
    reward_risk_min: 1.8
    risk_per_trade_pct: 2.0
    theta_timeout_minutes: 90
risk:
  daily_max_loss_pct: 3.0
  daily_profit_target_pct: 2.5
  max_open_positions: 1
  reward_risk_min: 2.5
  risk_per_trade_pct: 2.0
  squareoff_time: '15:15'
  theta_timeout_minutes: 90
